import React from 'react';
import Header from '../../SharedComponent/Header/Header';
import './Login.css';

const Login = () => {
    return (
        <div>
            <Header></Header>
            <h1>This is login</h1>
        </div>
    );
};

export default Login;